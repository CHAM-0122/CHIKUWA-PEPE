services:
  app:
    env_file:
      - .env.production
    restart: unless-stopped

  db:
    restart: unless-stopped

  nginx:
    restart: unless-stopped
    volumes:
      - ./nginx/http.conf:/etc/nginx/conf.d/default.conf:ro
      - ./uploads:/app/uploads:ro
